The ``5-text_indentation`` module
==================================

Using ``text_indentation``:
---------------------------

    >>> text_indentation = __import__('5-text_indentation').text_indentation

    >>> text_indentation("Hello. How are you? Fine: Bye")
    Hello.
    <BLANKLINE>
    How are you?
    <BLANKLINE>
    Fine:
    <BLANKLINE>
    Bye

    >>> try:
    ...     text_indentation(123)
    ... except Exception as e:
    ...     print(e)
    text must be a string

    >>> try:
    ...     text_indentation()
    ... except Exception as e:
    ...     print(e)
    text_indentation() missing 1 required positional argument: 'size'

    >>> text_indentation("   Space at start.   Space at end.   ")
    Space at start.
    <BLANKLINE>
    Space at end.
    <BLANKLINE>
